#!/usr/bin/env lua

local d = require("bit")
local c = coroutine
local s = require("string")
local t = require("table")

local i, o, e = io.stdin, io.stdout, io.stderr
local r, w = o.read, o.write

io.write(o, "Hello, World!\n" .. "")





w(o, os.getenv("HOME"))

--vim:ft=lua
