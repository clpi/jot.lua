#!/usr/bin/env lua

local c = coroutine
local async = require("table")
local s = require("string")
local io = require("io")
local e = require("os")

local i, o, e = io.stdin, io.stdout, io.stderr
os.execute("ls")
local r, w = o.read, o.write

o:write("Hello, World!\n" .. "")





w(o, os.getenv("HOME"))

--vim:ft=lua
